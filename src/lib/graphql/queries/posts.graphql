query Posts ($limit: Int!, $cursor: String) {
  posts(limit: $limit, cursor: $cursor) {
    hasMore
    posts{ 
      id
      createdAt
      updatedAt
      title
      content
      contentSnippet
      points
      creator {
        id
        username
      }
    }
  }
}
